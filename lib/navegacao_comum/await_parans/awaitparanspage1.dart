import 'package:flutter/material.dart';
    
class Awaitparanspage1 extends StatelessWidget {

  const Awaitparanspage1({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Center(child: Column(children: [
        Text("await parans page")
      ],),),
    );
  }
}