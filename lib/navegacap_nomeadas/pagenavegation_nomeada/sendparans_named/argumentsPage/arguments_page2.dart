import 'package:flutter/material.dart';
    
class ArgumentsPage2 extends StatelessWidget {

  const ArgumentsPage2({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('path Parans page 2'),
      ),
      body: Container(),
    );
  }
}